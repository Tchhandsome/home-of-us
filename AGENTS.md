# Home Of Us 项目级 Agent 指南

本文件是本项目的专属协作约定，用于补充全局默认 AGENTS 规则。默认 AGENTS 不允许修改；如两者存在冲突，以更严格、更安全、更贴合本项目长期维护的规则为准。

## 1. 项目定位

Home Of Us 是面向两人小家庭的生活管理系统，核心目标是让日常记录、提醒、整理和复盘变得自然、可靠、可持续。

项目第一阶段不追求“大而全”，优先解决高频真实场景：

- 手机端快速记录。
- 花卉养护记录与提醒。
- 家务、购物、库存、账本等家庭协作。
- 统一提醒中心。
- 统一资料库与附件管理。

健康档案、药品管理、可穿戴设备接入等能力暂不纳入 V1，后续在核心生活模块稳定后再规划。

## 2. 技术路线

### 2.1 总体架构

项目采用模块化单体架构，不使用微服务。

推荐结构：

```text
mobile-web / iOS App
        |
admin-web
        |
Spring Boot API
        |
MySQL
```

说明：

- 后端使用 Spring Boot。
- 前端使用 Vue3。
- 数据库使用 MySQL 8。
- 本地开发通过 Docker Compose 启动 MySQL。
- 手机端优先采用移动端 Web/PWA 形态，后续可通过 Capacitor 打包为 iOS App。
- App Store 发布不是 V1 必需项，等产品稳定后再评估。

### 2.2 微服务原则

当前阶段禁止为了架构形式拆微服务。

只有满足以下条件时才重新评估微服务：

- 多人团队长期并行开发，模块发布节奏明显不同。
- 通知、附件、AI 解析、设备同步等能力出现独立伸缩需求。
- 单体部署、编译、测试或数据隔离已经成为明确瓶颈。

在此之前，优先通过清晰包结构、领域边界、接口分层和数据库约束保持可维护性。

## 3. V1 模块边界

V1 重点建设以下业务模块。

### 3.1 首页/今日

聚合当天最重要的信息：

- 今日待办。
- 今日家务。
- 待购物品。
- 待处理提醒。
- 花卉养护提醒。
- 本月支出概览。

首页只展示关键摘要，不承载复杂编辑逻辑。

### 3.2 家庭空间

管理家庭基础信息：

- 家庭信息。
- 家庭成员。
- 成员角色。
- 成员偏好。

系统初期只面向两人小家庭设计，但数据模型需要支持后续扩展到更多成员。

### 3.3 快速记录

这是移动端的核心入口。

支持像聊天一样输入自然语言，例如：

```text
7.3 常春花是粉色的，用2号，7.10可用通用或者花卉型
```

V1 要求：

- 必须保存原始文本。
- 可以先做人工确认式结构化，不强依赖 AI。
- 支持关联到花卉、家务、购物、账本等业务对象。
- 支持从记录派生提醒。

任何解析失败都不能丢失原始记录。

### 3.4 提醒中心

统一管理所有业务模块产生的提醒：

- 一次性提醒。
- 重复提醒。
- 延后提醒。
- 完成记录。
- 过期提醒。

提醒中心应作为基础能力被其他模块复用，避免各业务模块各自实现提醒逻辑。

### 3.5 花卉档案

管理每一盆植物的长期档案：

- 名称。
- 品种。
- 花色。
- 摆放位置。
- 照片。
- 入手日期。
- 当前状态。
- 养护偏好。

花卉档案是 V1 的重点模块，手机端体验优先级高于管理端。

### 3.6 花卉养护记录

记录植物生命周期事件：

- 浇水。
- 施肥。
- 修剪。
- 换盆。
- 开花。
- 病虫害。
- 状态观察。
- 下次养护建议。

每条养护记录应保留发生日期、记录人、原文备注和结构化字段。

### 3.7 家务任务

管理家庭协作任务：

- 固定家务。
- 临时任务。
- 周期任务。
- 负责人。
- 完成情况。
- 历史记录。

家务模块不做复杂绩效评分，避免把生活工具做成压力来源。

### 3.8 购物清单

管理待买事项：

- 待买清单。
- 已买记录。
- 常购物品。
- 分类。
- 购买渠道。

购物清单需要适合手机端快速勾选。

### 3.9 家庭库存

管理家庭常用品库存：

- 日用品。
- 清洁用品。
- 食材。
- 消耗品。
- 低库存提醒。

库存模块应能与购物清单联动，库存不足时可生成待买事项。

### 3.10 家庭账本

记录家庭收入和支出：

- 支出记录。
- 收入记录。
- 分类。
- 成员。
- 共同支出。
- 个人支出。
- 月度统计。

V1 优先支持手动记账和基础统计，不做复杂报销、资产、投资和多币种能力。

### 3.11 资料库

统一管理家庭资料和附件：

- 花卉照片。
- 票据。
- 说明书。
- 保修单。
- 合同。
- 其他家庭资料。

资料库作为基础能力提供附件引用，不应把所有业务文件硬塞到单一表单里。

## 4. 基础能力模块

基础能力必须服务业务，不单独做复杂平台化。

推荐基础模块：

- 账号与权限。
- 文件附件。
- 标签与搜索。
- 通知与提醒。
- 数据导入导出。
- 操作日志。
- 系统设置。

其中提醒、附件、标签、搜索要尽早设计通用模型，避免后续多模块重复造轮子。

## 5. 后端设计约定

### 5.1 包结构建议

后端按领域拆包：

```text
com.homeofus
  common
  family
  record
  reminder
  plant
  chore
  shopping
  inventory
  finance
  attachment
  system
```

每个领域内部按职责分层：

```text
controller
service
repository
entity
dto
converter
enums
```

### 5.2 编码规则

- Java 对象判空必须使用 `Objects.isNull()`、`Objects.nonNull()` 或 `Objects.requireNonNull()`。
- 字符串判空必须使用 `StringUtils.isBlank()`、`StringUtils.isEmpty()` 等工具方法。
- 禁止直接使用 `== null` 或 `!= null`。
- 优先使用 Lombok 的 `@Getter`、`@Setter`、`@Builder`、`@NoArgsConstructor`、`@AllArgsConstructor`。
- 业务异常不允许硬编码中文异常信息，后续接入 I18n key。
- 关键业务方法需要 JavaDoc。
- 方法内部关键逻辑需要中文行注释。
- 类注释作者统一使用 `tanchaohong`。

### 5.3 API 约定

- API 命名保持业务语义清晰。
- 移动端和管理端共用同一套后端 API。
- 不为不同端复制业务接口。
- 查询接口需要考虑分页、排序和筛选。
- 删除类操作优先采用逻辑删除。
- 涉及重要数据变更时记录操作日志。

## 6. 数据库设计约定

- 数据库使用 MySQL 8。
- 表名使用小写下划线。
- 主键优先使用 `BIGINT`。
- 每张业务表应包含创建时间、更新时间、创建人、更新人、逻辑删除字段。
- 高频查询字段必须考虑索引。
- 金额字段使用 `DECIMAL`，禁止使用浮点类型。
- 业务枚举要在代码中集中定义，数据库存储稳定编码。
- 涉及跨模块引用时优先保存业务对象 ID，避免冗余大对象。

## 7. 前端设计约定

### 7.1 端侧职责

移动端优先解决：

- 快速记录。
- 今日事项。
- 花卉养护。
- 购物勾选。
- 提醒处理。

管理端优先解决：

- 批量整理。
- 表格筛选。
- 数据统计。
- 档案维护。
- 导入导出。

### 7.2 体验原则

- 手机端必须少输入、少跳转、可单手操作。
- 重要操作不依赖复杂菜单。
- 快速记录入口应始终容易到达。
- 花卉、购物、家务等高频模块优先做移动端体验。
- 管理端保持安静、清晰、适合长期维护数据。

## 8. 后续可扩展方向

V1 稳定后，可以考虑增加：

- 健康档案。
- 药箱管理。
- 家庭纪念日。
- 宠物管理。
- 菜谱与做饭计划。
- 旅行计划。
- 家庭目标和愿望清单。
- 订阅服务和会员到期提醒。
- 家电保养与维修记录。
- AI 辅助自然语言解析。
- iOS App 打包与 TestFlight 内测。

扩展新模块前必须先判断是否能复用对象档案、事件记录、提醒中心、附件、标签等基础能力。

## 9. 开发节奏

推荐第一版只做最小可用闭环：

1. Docker Compose 启动 MySQL。
2. Spring Boot 后端骨架。
3. Vue3 移动端骨架。
4. Vue3 管理端骨架。
5. 家庭空间。
6. 快速记录。
7. 提醒中心。
8. 花卉档案与养护记录。
9. 购物清单。
10. 基础资料库。

完成第一版闭环后，再扩展家务、库存、账本等模块。

## 10. 协作纪律

- 未经用户明确允许，不执行任何 Git 操作。
- 不修改全局默认 AGENTS。
- 不为了形式引入复杂架构。
- 不在 V1 中实现暂缓模块。
- 不做与当前目标无关的大规模重构。
- 每次变更优先保持小步、可验证、可回退。

